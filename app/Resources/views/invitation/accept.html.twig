{% extends 'base.html.twig' %}
{% block title %}{{ 'invitation.accept.title'|trans }}{% endblock %}
{% block body %}
    <h1>{{ 'invitation.accept.title'|trans }}</h1>

	<p>{{ 'invitation.accept.confirmation'|trans }}</p>
    {{ form_start(form) }}
        <input type="submit" value="{{ 'invitation.accept.form.submit'|trans }}" />
        <a href="{{ path('invitation_index') }}">{{ 'invitation.accept.back'|trans }}</a>
    {{ form_end(form) }}
{% endblock %}
