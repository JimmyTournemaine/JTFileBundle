{% extends 'base.html.twig' %}
{% block title %}{{ 'invitation.decline.title'|trans }}{% endblock %}
{% block body %}
    <h1>{{ 'invitation.decline.title'|trans }}</h1>

	<p>{{ 'invitation.decline.confirmation'|trans }}</p>
    {{ form_start(form) }}
        <input type="submit" value="{{ 'invitation.decline.form.submit'|trans }}" />
        <a href="{{ path('invitation_index') }}">{{ 'invitation.decline.back'|trans }}</a>
    {{ form_end(form) }}
{% endblock %}
