{% extends 'base.html.twig' %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value={{ 'contact.labels.send'|trans({}, 'JTContactUsBundle') }}>
	{{ form_end(form) }}
{% endblock body %}